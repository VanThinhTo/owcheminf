version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true

submodules:
  include: all
  recursive: false

python:
  install:
    - requirements: docs/requirements.txt
